# Vite reads this at build time. Real values live in spa/.env (gitignored).
# Source values from tomato-tracker/.env.
VITE_GAS_API_URL=https://script.google.com/macros/s/.../exec
VITE_HARVEST_TOKEN=...
VITE_CLAIMS_TOKEN=...
